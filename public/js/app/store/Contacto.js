/**
 * Define the store for the data
 */
Ext.define('CttList.store.Contacto', {
	/** Extend the default data Store */
	extend: 'Ext.data.Store',
	/** Link to a model for structure */
	model: 'CttList.model.Contacto',
});